node_modules
.next
out
.env
.env.local
*.log
.DS_Store
*.tsbuildinfo
/storage
/test-results
/playwright-report
/blob-report
/playwright/.cache

.vercel
.env*

# Fase 4F, item 9 -- artefatos de execução real do harness de benchmark do Assistente RAIZ (nunca
# segredo/chave, só scorecard/respostas -- ver src/lib/ai/benchmark/artifact-store.ts). Locais/reproduzíveis
# a qualquer momento rodando o benchmark de novo -- não faz sentido versionar.
/.benchmark-runs
