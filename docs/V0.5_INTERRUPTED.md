# v0.5 — Trabalho interrompido / não homologado

## Atualização (2026-09-03)

Este documento descreve o estado no momento em que o desenvolvimento foi transferido de ambiente
(fim de 2026-09-01). Desde então, o trabalho continuou na mesma sessão do Claude Code e boa parte do
que este arquivo listava como pendente já foi endereçado — ver `docs/PROJECT_STATE.md` para o estado
atual e completo (é sempre a fonte de verdade mais recente). O conteúdo original abaixo foi mantido
como registro histórico; a lista de pendências ao final tem uma nota de status em cada item.

## Motivo deste arquivo

O desenvolvimento foi transferido de ambiente no meio da v0.5. Este documento evita que código parcialmente implementado seja confundido com versão concluída.

## Intenção da v0.5

Fechar a cadeia de campo real:

`Talhão → Ordem de coleta → Grid/pontos → GPS → Coleta → Amostra → Resultado laboratorial`

## Código já iniciado

### Migration 004

Adiciona metadados de estratégia de amostragem, sequência, posição observada, precisão GPS, payload de origem, índices e `updated_at` para ordens de coleta.

### Parser de pontos

`src/domain/field-operations.ts`

- CSV com delimitador `;`, `,` ou TAB;
- coordenadas com decimal brasileiro;
- GeoJSON FeatureCollection de Point;
- aliases de cabeçalho;
- bloqueio de código/coordenada duplicados;
- validação geográfica básica.

### Operações persistentes

`src/lib/repositories/collections.ts`

- listar ordens;
- criar ordem;
- gerar pontos GRID via PostGIS;
- importar pontos CSV/GeoJSON;
- conferir ponto dentro do talhão;
- registrar posição observada do GPS;
- medir distância para o ponto planejado;
- atualizar progresso/estado;
- auditoria.

### APIs

- `GET/POST /api/collection-orders`
- `POST /api/collection-orders/[id]/points`
- `PATCH /api/collection-orders/[id]/points/[pointId]`

### UI

Há um primeiro `field-operations-manager.tsx` e alterações na tela Coletas. Auditar responsividade, estados e integração real antes de preservar como interface final.

## Validação já executada

`node --experimental-strip-types scripts/test-field-operations.mjs`

Resultado: **4 cenários aprovados** para parser CSV/GeoJSON.

Isso **não valida** banco, PostGIS, API, RLS, GPS nem UI.

## Pontos que o próximo executor precisa revisar

Status de cada item, adicionado em 2026-09-03 (ver commits `72bcdfa`, `c5a2ff3`, `d0f29cd` na branch
`develop` e a seção correspondente em `docs/PROJECT_STATE.md`):

- ✅ migration 004 deve ser adicionada ao checker estrutural — já estava feito (`scripts/check-migrations.mjs`).
- ✅ verificar se as tabelas/colunas de 001 realmente são compatíveis com todas as queries novas — validado indiretamente: a nova suíte e2e (`e2e/field-operations-isolation.spec.ts`) executa as queries reais de `collections.ts` contra o banco de dev real, sem erro de schema.
- ✅ executar `ST_SquareGrid` e transformação UTM em PostGIS real — já validado em bloco anterior desta sessão (grid real de 81 pontos existe no banco de dev).
- ⏳ revisar comportamento em talhões que cruzam zonas UTM ou possuem geometria complexa — **ainda não revisado**. A zona UTM é calculada só pelo centróide do talhão; um talhão real de fazenda não deve se aproximar de 6° de largura, mas isso não foi comprovado com um caso real, só argumentado.
- ✅ revisar limite arbitrário de 2.000 pontos — nomeado como `MAX_POINTS_PER_ORDER` em `collections.ts` (era dois literais soltos). O valor em si continua arbitrário, sem embasamento agronômico — revisar o número é decisão de produto, não técnica.
- ✅ avaliar performance da validação de pontos importados — não há N+1: a validação "ponto dentro do talhão" e o insert em massa já são set-based (uma query com `unnest`), confirmado lendo o código.
- ✅ testar concorrência/reimportação — coberto em `e2e/field-operations-isolation.spec.ts`.
- ✅ testar ordem com coleta já iniciada — coberto no mesmo arquivo.
- ⏳ testar precisão GPS e regras de distância com casos reais — testado só o caso feliz (coleta a 0m do ponto planejado). Falta um teste dedicado do limite de distância (`greatest(75m, precisão×2, lado do grid/2)` em `collectSamplePoint`).
- ⏳ validar RBAC por função — **ainda não automatizado**. As 4 contas de teste (`rbac-agronomist/field-tech/commercial/viewer@raiz.local`) existem no banco com o papel certo, mas a senha delas não é conhecida nesta sessão e redefini-la exige escrita direta no banco, que o modo automático corretamente bloqueou sem aprovação humana. A checagem de papel em si existe e foi lida no código (`writeRoles` em cada rota), só falta o teste automatizado.
- ✅ validar RLS cross-tenant — coberto a fundo em `e2e/field-operations-isolation.spec.ts` (leitura e 4 tentativas de escrita, todas confirmadas como 404, dado da empresa A confirmado intacto depois).
- ✅ criar vínculo formal entre `sample_points` e resultados laboratoriais quando ainda ausente — o vínculo (FK via `lab_samples.sample_point_id`) já existia; o que faltava era `listCollectionOrders` usar esse FK em vez de um casamento de texto paralelo e frágil — corrigido.
- não adicionar interpolação cartográfica até haver critério técnico homologado — mantido, nenhuma mudança nessa área.
